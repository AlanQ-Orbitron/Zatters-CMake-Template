# Zatter's CMake Template [^1]

A CMake Template, reusable with blank files and other reusable media.

[^1]: I got tired of adding CMake stuff to my project, so here's a template.

**setup.sh**
> Changes the Template name in CMake to the current directory's name.

**build.sh**
> Builds from src/main.cpp into the build folder.
